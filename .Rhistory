shiny::runApp()
runApp()
runApp()
# APP CATALOG (META DATA) ----
app1 <- list(
title = "Wedding Budget Risk Analyzer",
subtitle = "Python",
description = "A financial application for analyzing trends in your favorite SP 500 stocks. Leverages AWS EC2 and MongoDB Atlas Cloud.",
sub_directory = "stock_analyzer_mongo_atlas",
tags = tibble(
tag = c("Python", "Shiny", "AWS"),
color = c("info", "success", "default")
) %>% list(),
img = "wedding_risk.png"
)
app_catalog_tbl <- bind_rows(app1, .id = "id")
runApp()
runApp()
devtools::build_readme()
install.packages("gitignore")
f <- file.path(tempdir(), ".gitignore")
new_lines <- gi_fetch_templates("r")
gi_write_gitignore(fetched_template = new_lines, gitignore_file = f)
library(gitignore)
f <- file.path(tempdir(), ".gitignore")
new_lines <- gi_fetch_templates("r")
gi_write_gitignore(fetched_template = new_lines, gitignore_file = f)
getwd()
f <- file.path(tempdir(), ".gitignore")
new_lines <- gi_fetch_templates("r")
gi_write_gitignore(fetched_template = new_lines, gitignore_file = f)
tempdir()
f <- file.path(getwd(), ".gitignore")
new_lines <- gi_fetch_templates("r")
gi_write_gitignore(fetched_template = new_lines, gitignore_file = f)
gi_write_gitignore(fetched_template = new_lines, gitignore_file = f)
